# Resumo Para GitHub

```text
Core RAG técnico da Opiagile para produtos e gateways de atendimento com IA.

Demonstra upload de documentos TXT, chunking, respostas com fontes, embeddings/pgvector quando configurados, LLM real validada, memória conversacional, triagem de intenção, lead, handoff humano, observabilidade, workflows n8n e contrato para frontend/gateways separados.

Posicionamento: core RAG com fontes, fallback textual local, pgvector com embeddings reais quando configurado e modo local sem chave externa.
```
